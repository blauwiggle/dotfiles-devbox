# Linux-side sysctl for heavy Claude Code ops (installed to /etc/sysctl.d/).
# Multiple VS Code servers + Claude Code instances watch many files; the default
# inotify limits trigger "ENOSPC: System limit for number of file watchers reached".
fs.inotify.max_user_watches=524288
fs.inotify.max_user_instances=1024
